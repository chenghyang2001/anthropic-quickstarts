# 訂閱版（Subscription edition）— 不需要 ANTHROPIC_API_KEY
#
# 本版本透過 @anthropic-ai/claude-agent-sdk 走 Claude Code CLI 的 OAuth，
# 使用你的 Claude Pro/Max 訂閱額度，不扣 API Credits。
#
# 先決條件：
# 1. 本機已安裝 claude CLI（claude --version 能跑）
# 2. 已透過 `claude` 互動模式登入過（OAuth 憑證存在 ~/.claude/.credentials.json）
# 3. 全域環境變數 ANTHROPIC_API_KEY 必須「不存在」或為空字串，
#    否則 SDK 會優先走 API 模式，訂閱版就失效。
#
# 如果你目前 shell 有設 ANTHROPIC_API_KEY，啟動 dev server 前請先：
#   PowerShell:  Remove-Item Env:ANTHROPIC_API_KEY
#   Git Bash:    unset ANTHROPIC_API_KEY
#
# 限制（POC 範圍）：
# - 只支援純文字對話 + chart 生成
# - 圖片/PDF 上傳 UI 保留但後端會跳過（送出後僅取文字部分）
# - 只能本機跑（Vercel 等雲端沒有 claude CLI + OAuth 憑證）
